<div class="container">
  <h2 class="page-title">All Boards</h2>
  <div class="cards">
    <% @boards.each do |board| %>
      <%= render 'board', board: board %>
    <% end %>
    <%= link_to new_board_path do %>
      <div class="add-new-card">
        <p class="add-new-card_text">+ Add new board</p>
      </div>
    <% end %>
  </div>
</div>
