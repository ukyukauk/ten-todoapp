<div class="container">
  <h2 class="page-title">
    New Card
  </h2>
  <ul class="errorMessages">
    <% @task.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <%= form_with(model: @task, url: board_tasks_path(@board), local: true) do |f| %>
    <div class="field">
      <%= f.label :deadline %>
    </div>
    <div>
      <%= f.date_field :deadline, class: 'deadline' %>
    </div>
    <div class="field">
      <%= f.label :eyecatch %>
    </div>
    <div>
      <%= f.file_field :eyecatch, class: 'eyecatch' %>
    </div>
    <div class="field">
      <%= f.label :name %>
    </div>
    <div>
      <%= f.text_field :name, class: 'text' %>
    </div>
    <div class="field">
      <%= f.label :description %>
    </div>
    <div>
      <%= f.text_area :description %>
    </div>
    <%= f.submit 'Submit', class: 'btn-primary' %>
  <% end %>
</div>
